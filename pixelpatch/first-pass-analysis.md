# Trace Compare first-pass

- referenceMode: `direct_product_screenshot`; source: `pixelpatch/reference.png`.
- Code-first regions: shell/sidebar, session selectors, comparison validity, summary metrics, outcome evidence, efficiency/cost, reliability, context efficiency, cost drivers, divergence points, synchronized A/B trajectory, behavior delta, behavioral patterns and recommendations.
- Model-generated assets: none. Icons use the installed Lucide set; bars, tables and trajectory previews are CSS/SVG/DOM.
- Selected capability pack: `Production React Pack`, because the repository already uses React/Vite and the target is a dense analytical dashboard.
- Stack choices: existing React components; vanilla CSS grid and tokens; native controls; deterministic SVG trajectory preview; responsive CSS breakpoints; no new dependency.
- Data policy: missing fields render `—`, `未知` or an explicit empty state; cost is not invented when source data is unavailable.
- Region coverage: all visible Trace Compare sections map to selectors in `pixelpatch/region-map.json`; no asset slots are required.
