import { sanitizeComparisonReport, type ComparisonReport, type MetricDelta, type ReportSide } from './comparison-report.js'
import { createElement } from 'react'
import { renderToStaticMarkup } from 'react-dom/server'
import { SessionArcDiagram } from '../src/features/compare/SessionArcDiagram.tsx'

const esc = (value: unknown) => String(value ?? '').replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;')
const finite = (value: unknown): number | null => typeof value === 'number' && Number.isFinite(value) ? value : null
const signed = (value: number) => value > 0 ? '+' + value : String(value)
const formatInteger = (value: number | null) => value == null ? '—' : new Intl.NumberFormat('en-US', { maximumFractionDigits: 0 }).format(value)
const formatTokens = (value: number | null) => {
  if (value == null) return '—'
  const absolute = Math.abs(value)
  return absolute >= 1_000_000 ? (value / 1_000_000).toFixed(2) + 'M' : absolute >= 1_000 ? (value / 1_000).toFixed(absolute >= 100_000 ? 0 : 1) + 'K' : formatInteger(value)
}
const formatDuration = (value: number | null) => {
  if (value == null) return '—'
  if (value < 1_000) return Math.round(value) + 'ms'
  const seconds = Math.round(value / 1_000)
  return seconds >= 60 ? Math.floor(seconds / 60) + 'm ' + String(seconds % 60).padStart(2, '0') + 's' : seconds + 's'
}
const formatPercent = (value: number | null) => value == null ? '—' : (value * 100).toFixed(1) + '%'
const formatPp = (value: number | null) => value == null ? '—' : signed(Number(value.toFixed(1))) + 'pp'

type MetricKind = 'duration' | 'tokens' | 'integer' | 'ratio' | 'percent'
const metricValue = (metric: MetricDelta, kind: MetricKind, side: 'a' | 'b') => {
  const value = finite(metric[side])
  if (kind === 'duration') return formatDuration(value)
  if (kind === 'tokens') return formatTokens(value)
  if (kind === 'ratio' || kind === 'percent') return formatPercent(value)
  return formatInteger(value)
}
const metricDelta = (metric: MetricDelta, kind: MetricKind) => {
  if (metric.a == null || metric.b == null) return '—'
  if (kind === 'ratio') return formatPp(metric.percentagePointDelta ?? null)
  if (kind === 'percent' || kind === 'duration' || kind === 'tokens') return metric.percentDelta == null ? '—' : signed(Number((metric.percentDelta * 100).toFixed(1))) + '%'
  return metric.absoluteDelta == null ? '—' : signed(Number(metric.absoluteDelta.toFixed(1)))
}
const colors: Record<string, string> = { system: '#3656a8', developer: '#36a9bd', skills: '#7659d9', tools: '#d9922e', permissions: '#d86b78', environment: '#2d9e93', apps: '#8b6bdc', userHistory: '#8f9dad' }
const labels: Record<string, string> = { system: 'System', developer: 'Developer', skills: 'Skills', tools: 'Tools', permissions: 'Permissions', environment: 'Environment', apps: 'Apps', userHistory: 'User history' }
function renderStack(side: ReportSide, metrics: ComparisonReport['context']['baseline']) {
  const known = metrics.composition.filter(item => item.share != null)
  if (!known.length) return '<div class="stack-row"><b>' + side + '</b><div class="stack unavailable">DATA UNAVAILABLE</div><small>—</small></div>'
  const bars = known.map(item => '<i title="' + esc(labels[item.category] || item.category) + '" style="width:' + Math.max(0, Math.min(100, (item.share || 0) * 100)) + '%;background:' + (colors[item.category] || '#8f9dad') + '"></i>').join('')
  return '<div class="stack-row"><b>' + side + '</b><div class="stack">' + bars + '</div><small>' + formatTokens(metrics.promptFootprintTotal) + '</small></div>'
}

function renderContext(report: ComparisonReport) {
  const rows = report.context.compositionDelta.filter(item => item.aTokens != null || item.bTokens != null).slice(0, 5).map(item => '<div class="context-row"><span>' + esc(labels[item.category] || item.category) + '</span><b>' + formatTokens(item.aTokens) + '</b><b>' + formatTokens(item.bTokens) + '</b><em>' + formatPp(item.shareDeltaPp) + '</em></div>').join('')
  const legend = Object.keys(labels).filter(key => report.context.baseline.composition.some(item => item.category === key && item.share != null) || report.context.candidate.composition.some(item => item.category === key && item.share != null)).map(key => '<span><i style="background:' + colors[key] + '"></i>' + esc(labels[key]) + '</span>').join('')
  return '<div class="stack-pair">' + renderStack('A', report.context.baseline) + renderStack('B', report.context.candidate) + '</div><div class="legend">' + legend + '</div><div class="context-table"><div class="context-head"><span>Known footprint</span><b>A</b><b>B</b><em>Δ share</em></div>' + (rows || '<div class="empty">数据不足 · DATA UNAVAILABLE</div>') + '</div><p class="annotation">BASED ON KNOWN PROMPT FOOTPRINT</p>'
}

const kpiCss = '.kpi-grid{display:grid;grid-template-columns:repeat(6,1fr);gap:1px;background:var(--grid)}.kpi{background:var(--bg);padding:17px 15px;min-height:105px}.kpi span{display:block;color:var(--mut);font-size:9px;line-height:1.35}.kpi b{display:block;font-size:18px;line-height:1.1;margin:14px 0 7px}.kpi em{font-style:normal;font-size:11px;color:var(--data)}'
const css = [
  kpiCss,
  ':root{--bg:#f7f2eb;--txt:#081f5c;--mut:rgba(8,31,92,.6);--lab:rgba(8,31,92,.72);--faint:rgba(8,31,92,.32);--quiet:rgba(8,31,92,.15);--grid:rgba(8,31,92,.16);--data:#334eac;--data2:#7096d1;--hero:#081f5c;--panel:rgba(8,31,92,.045)}',
  '*{box-sizing:border-box}html{background:#dcd5cb}body{margin:0;background:var(--bg);color:var(--txt);font-family:Inter,-apple-system,BlinkMacSystemFont,"Segoe UI","PingFang SC","Hiragino Sans GB","Microsoft YaHei",sans-serif;font-size:13px;line-height:1.5}.sheet{width:1080px;max-width:100%;margin:0 auto;padding:54px 60px 70px;background:var(--bg)}',
  'h1,h2,h3,p{margin:0}h1{font-size:52px;line-height:1.06;letter-spacing:-.045em;font-weight:650;max-width:680px}h2{font-size:24px;letter-spacing:-.025em;font-weight:650}.kicker,.eyebrow,.subhead{font-size:10px;letter-spacing:.16em;font-weight:750;color:var(--mut)}.kicker{margin-bottom:7px}.subhead{color:var(--hero);font-size:11px;margin-bottom:16px}.subhead span{font-size:11px;font-weight:500;letter-spacing:0;margin-left:8px;color:var(--mut)}',
  '.masthead{display:flex;justify-content:space-between;border-top:1px solid var(--txt);padding-top:12px;margin-bottom:58px}.brand{font-size:12px;font-weight:800;letter-spacing:.12em}.report-name{text-align:right;font-size:10px;line-height:1.3;letter-spacing:.14em;color:var(--mut)}.hero{padding-bottom:52px;border-bottom:1px solid var(--grid)}.hero p{max-width:590px;color:var(--mut);font-size:16px;margin-top:22px}.hero-verdict{margin-top:34px;padding:19px 22px;background:var(--hero);color:#fff;display:flex;align-items:center;gap:22px}.hero-verdict b{font-size:10px;letter-spacing:.14em;color:#bad6eb}.hero-verdict span{font-size:18px}.hero-verdict.invalid{background:#8b3945}.hero-meta{display:flex;gap:28px;margin-top:20px;color:var(--mut);font-size:11px}.hero-meta b{color:var(--txt);font-weight:650;margin-right:5px}',
  '.section{padding:42px 0;border-bottom:1px solid var(--grid)}.section-head{display:flex;justify-content:space-between;margin-bottom:22px}.validity{display:grid;grid-template-columns:1fr 180px;gap:38px}.validity-grid{display:grid;grid-template-columns:repeat(4,1fr);border-top:1px solid var(--grid);border-left:1px solid var(--grid)}.validity-grid div{padding:12px 13px;border-right:1px solid var(--grid);border-bottom:1px solid var(--grid)}.validity-grid span{display:block;color:var(--mut);font-size:11px}.validity-grid b{display:block;margin-top:4px;font-size:11px}.validity-grid .same{color:#277e70}.validity-grid .different{color:#aa6024}.validity-grid .unknown{color:var(--mut)}.confidence{background:var(--panel);padding:17px}.confidence strong{display:block;font-size:40px;line-height:1.1;margin:8px 0}.confidence em{font-style:normal;font-size:11px;letter-spacing:.14em}.validity-note{grid-column:1/-1;color:var(--mut);font-size:12px;margin-top:15px}.warning-list{margin:10px 0 0;padding-left:18px;color:#8b3945;font-size:12px}',
  '.glance{display:grid;grid-template-columns:1fr 1fr;gap:16px}.panel{background:var(--panel);padding:21px 22px;min-height:255px}.panel-head{display:flex;justify-content:space-between;align-items:baseline;margin-bottom:16px}.panel-head h3{font-size:16px}.panel-head span{font-size:10px;letter-spacing:.12em;color:var(--mut)}.axis-key{display:flex;justify-content:space-between;color:var(--mut);font-size:10px;margin-bottom:12px}.swatch{display:inline-block;width:18px;height:3px;margin:0 5px 3px 0;background:var(--data)}.swatch.b{background:var(--data2)}.delta-row{display:grid;grid-template-columns:104px 1fr 60px;gap:9px;align-items:center;height:28px;font-size:11px}.delta-axis{position:relative;height:14px;border-bottom:1px solid var(--quiet)}.delta-axis:after{content:"";position:absolute;left:50%;top:0;height:14px;border-left:1px solid var(--grid)}.delta-axis i{position:absolute;top:5px;height:5px;border-radius:2px}.delta-axis i.a{background:var(--data)}.delta-axis i.b{background:var(--data2)}.delta-axis em{position:absolute;right:2px;top:-2px;font-style:normal;color:var(--mut);font-size:9px}.delta-row>b{text-align:right;font-weight:650}.unavailable{color:var(--mut);font-size:9px;letter-spacing:.04em}.winner-note{border-top:1px dotted var(--grid);margin-top:15px;padding-top:10px;color:var(--mut);font-size:11px}',
  'table{width:100%;border-collapse:collapse}.matrix th,.matrix td,.metric-table th,.metric-table td{padding:9px 7px;border-bottom:1px solid var(--grid);text-align:left}.matrix thead th,.metric-table thead th{font-size:10px;letter-spacing:.12em;color:var(--mut);font-weight:650}.matrix td{text-align:center;font-size:9px;letter-spacing:.08em}.matrix td.passed{color:#277e70}.matrix td.failed{color:#a83f4d}.matrix td.unknown,.matrix td.not_applicable{color:var(--mut)}.matrix th{font-weight:500}.coverage{display:flex;gap:20px;margin-top:14px;color:var(--mut);font-size:11px}.coverage b{color:var(--txt)}.notice{margin-top:14px;color:#8b3945;font-size:11px;letter-spacing:.04em}.stack-pair{margin:5px 0 15px}.stack-row{display:grid;grid-template-columns:18px 1fr 56px;gap:9px;align-items:center;margin:13px 0}.stack-row>b{font-size:12px}.stack-row small{text-align:right;color:var(--mut)}.stack{height:25px;display:flex;background:#e7e0d7;overflow:hidden}.stack i{height:100%;display:block}.stack.unavailable{padding:6px 9px;color:var(--mut);font-size:10px}.legend{display:flex;flex-wrap:wrap;gap:8px 14px;color:var(--mut);font-size:10px}.legend i{display:inline-block;width:8px;height:8px;margin-right:4px}.context-table{margin-top:18px}.context-head,.context-row{display:grid;grid-template-columns:1fr 65px 65px 54px;gap:8px;align-items:center;padding:7px 0;border-bottom:1px solid var(--grid);font-size:11px}.context-head{color:var(--mut);font-size:10px}.context-row b{text-align:right;font-weight:500}.context-row em{text-align:right;font-style:normal;color:var(--mut)}.annotation{margin-top:14px;font-size:9px;letter-spacing:.13em;color:var(--mut)}.empty{padding:28px 0;color:var(--mut);font-size:11px}.driver-columns,.behavior-columns{display:grid;grid-template-columns:1fr 1fr;gap:16px}.driver-columns>article,.behavior-columns>div{background:var(--panel);padding:21px 22px;overflow:hidden}.waterfall{width:100%;height:145px;overflow:visible}.waterfall line{stroke:var(--grid)}.waterfall rect.positive{fill:var(--data2)}.waterfall rect.negative{fill:#b96d63}.waterfall text{font-size:9px;fill:var(--mut)}.waterfall text.total{font-weight:700;fill:var(--txt)}',
  '.metric-table{background:var(--panel);font-size:12px}.metric-table th,.metric-table td{padding:10px 15px}.metric-table tbody th{font-weight:500}.metric-table tbody td{text-align:right}.metric-table tbody td.delta{color:var(--mut);font-weight:650}.divergence-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.divergence-card{background:var(--panel);padding:18px}.divergence-top{display:flex;align-items:center;gap:9px}.divergence-top>b{font-size:18px}.divergence-top span{font-size:10px;letter-spacing:.09em;color:var(--mut)}.divergence-top em{margin-left:auto;font-size:9px;font-style:normal;color:#277e70}.divergence-card>p{margin:16px 0;color:var(--mut);min-height:38px}.path{border-top:1px solid var(--grid);padding-top:9px;font-size:11px}.path span{display:block;margin:4px 0}.path b{display:inline-block;width:18px}.impact{border-top:1px dotted var(--grid);margin-top:13px;padding-top:9px}.impact span{display:block;font-size:9px;letter-spacing:.13em;color:var(--mut)}.impact b{display:block;margin-top:4px;font-size:11px}.trace{width:100%;height:auto;background:var(--panel);padding:12px}.trace-line{stroke:var(--grid);stroke-dasharray:2 5}.trace text{fill:var(--mut);font-size:10px}.trace circle.point-a{fill:var(--data)}.trace circle.point-b{fill:var(--data2)}.trace circle.compaction{fill:#d86b78}.behavior-row{display:grid;grid-template-columns:130px 1fr 78px;gap:9px;align-items:center;border-bottom:1px solid var(--grid);padding:8px 0;font-size:11px}.behavior-row>div{position:relative;height:13px;background:#e5ded5}.behavior-row i{display:block;position:absolute;left:0;height:5px}.behavior-row i.a{top:1px;background:var(--data)}.behavior-row i.b{bottom:1px;background:var(--data2)}.behavior-row>b{text-align:right;font-size:10px}.pattern{display:flex;justify-content:space-between;gap:12px;border-bottom:1px solid var(--grid);padding:11px 0}.pattern b{font-size:10px}.pattern span{color:var(--mut);font-size:11px;text-align:right}.pattern.high b{color:#a83f4d}.pattern.medium b{color:#a76a2b}.pattern.low b{color:var(--mut)}.recommendations{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.recommendations article{background:var(--panel);padding:18px}.recommendation-head{display:flex;align-items:center;gap:8px;border-bottom:1px solid var(--grid);padding-bottom:13px}.recommendation-head>b{font-size:9px;color:#a83f4d;letter-spacing:.12em}.recommendation-head h3{font-size:14px}.recommendation-head span{margin-left:auto;font-size:9px;color:var(--mut)}dl{margin:14px 0}dt{font-size:9px;letter-spacing:.12em;color:var(--mut);margin-top:10px}dd{margin:2px 0;font-size:11px}.recommendations footer{border-top:1px dotted var(--grid);padding-top:10px;color:var(--mut);font-size:9px;letter-spacing:.1em}.quality-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}.quality-grid>div{background:var(--panel);padding:15px 18px}.quality-grid>div>div{display:flex;justify-content:space-between;border-bottom:1px solid var(--grid);padding:8px 0}.quality-grid span{color:var(--mut)}.warnings{padding-left:18px;color:#8b3945;font-size:11px}.method{display:grid;grid-template-columns:repeat(3,1fr);gap:1px;background:var(--grid)}.method div{background:var(--bg);padding:14px}.method span{display:block;color:var(--mut);font-size:10px}.method b{display:block;margin-top:4px;font-size:12px}.footer-note{color:var(--mut);font-size:10px;margin-top:18px}',
  '@media (max-width:760px){.sheet{padding:32px 22px}.glance,.validity,.driver-columns,.behavior-columns,.quality-grid{grid-template-columns:1fr}.divergence-grid,.recommendations{grid-template-columns:1fr}h1{font-size:38px}.validity-grid{grid-template-columns:repeat(2,1fr)}.hero-meta{flex-wrap:wrap}}',
  '@media print{html,body{background:var(--bg);-webkit-print-color-adjust:exact;print-color-adjust:exact}.sheet{width:1080px;padding:35px 45px}.section,.panel,.divergence-card,.recommendations article,.driver-columns>article{break-inside:avoid;page-break-inside:avoid}.hero-verdict{print-color-adjust:exact}.section{padding-top:30px;padding-bottom:30px}}',
  '@media (prefers-reduced-motion:reduce){*,*:before,*:after{animation-duration:0s!important;transition-duration:0s!important;scroll-behavior:auto!important}}',
].join('')

function compactDeltaRows(entries: Array<{ label: string; metric: MetricDelta; kind: MetricKind }>) {
  const values = entries.map(item => Math.abs(item.kind === 'ratio' ? item.metric.percentagePointDelta || 0 : (item.metric.percentDelta || 0) * 100))
  const max = Math.max(1, ...values)
  return entries.map(item => {
    const delta = item.kind === 'ratio' ? item.metric.percentagePointDelta : item.metric.percentDelta == null ? null : item.metric.percentDelta * 100
    const width = delta == null ? 0 : Math.max(3, Math.abs(delta) / max * 100)
    return '<div class="measure-row"><span>' + esc(item.label) + '</span><div class="measure-track"><i class="' + (delta != null && delta > 0 ? 'up' : 'down') + '" style="width:' + width + '%"></i></div><b>' + esc(metricDelta(item.metric, item.kind)) + '</b></div>'
  }).join('')
}

function heroArc(report: ComparisonReport, invalid: boolean) {
  const verdict = invalid ? 'INCONCLUSIVE' : report.summary.judgement === 'a_dominates' ? 'A BETTER' : report.summary.judgement === 'b_dominates' ? 'B BETTER' : 'TRADE-OFF'
  return renderToStaticMarkup(createElement(SessionArcDiagram, {
    leftSession: { id: report.sessions.baseline.sessionId },
    rightSession: { id: report.sessions.candidate.sessionId },
    conclusion: verdict,
    deltaFocus: 'TRACE DELTA',
    deltaSummary: report.summary.headline || report.validity.summary,
  }))
}

function toolDots(calls: number | null, failureRate: number | null) {
  if (calls == null || calls <= 0) return '<span class="tool-na">DATA UNAVAILABLE</span>'
  const count = Math.min(6, Math.max(1, Math.round(calls)))
  const failures = Math.min(count, Math.max(0, Math.round(count * (failureRate || 0))))
  return Array.from({ length: count }, (_, index) => '<i class="tool-dot ' + (index >= count - failures ? 'fail' : '') + '">' + (index >= count - failures ? '×' : '✓') + '</i>').join('')
}

function renderReliabilityPanel(report: ComparisonReport) {
  const aCalls = finite(report.efficiency.toolCalls.a); const bCalls = finite(report.efficiency.toolCalls.b)
  const aRate = finite(report.reliability.toolSuccessRate.a); const bRate = finite(report.reliability.toolSuccessRate.b)
  return '<div class="panel-title"><h3>工具稳定性差异很明显</h3><span>TOOL RELIABILITY · SUCCESS / FAILURE</span></div><div class="tool-sides"><div><b>A</b><small>' + formatInteger(aCalls) + ' calls</small><p>' + toolDots(aCalls, finite(report.reliability.toolFailureRate.a)) + '</p><strong>' + formatPercent(aRate) + '</strong><em>tool success rate</em></div><div><b>B</b><small>' + formatInteger(bCalls) + ' calls</small><p>' + toolDots(bCalls, finite(report.reliability.toolFailureRate.b)) + '</p><strong>' + formatPercent(bRate) + '</strong><em>tool success rate</em></div></div>'
}

function renderContextTracePanel(report: ComparisonReport, title = '上下文压力与调用节奏不同') {
  const flows = report.tokenFlow.filter(item => finite(item.contextRatio) != null)
  const maxTime = Math.max(1, ...flows.map(item => finite(item.atMs) || 0))
  const points = (side: ReportSide) => flows.filter(item => item.side === side).map((item, index) => {
    const x = 18 + ((finite(item.atMs) || index) / maxTime) * 302
    const y = 132 - (finite(item.contextRatio) || 0) * 104
    return x.toFixed(1) + ',' + y.toFixed(1)
  }).join(' ')
  return '<div class="panel-title"><h3>' + esc(title) + '</h3><span>TOKEN / CONTEXT EVOLUTION · MODEL CALLS</span></div><svg class="mini-trace" viewBox="0 0 340 150" role="img" aria-label="context evolution"><line x1="18" y1="28" x2="322" y2="28"></line><line x1="18" y1="82" x2="322" y2="82"></line><line x1="18" y1="132" x2="322" y2="132"></line><polyline class="trace-a" points="' + points('A') + '"></polyline><polyline class="trace-b" points="' + points('B') + '"></polyline></svg><small class="chart-note">HAIRLINE TRACE · CONTEXT RATIO</small>'
}

function signalTile(label: string, caption: string, metric: MetricDelta, kind: MetricKind) {
  return '<article class="signal-tile"><b>' + esc(label) + '</b><span>' + esc(caption) + '</span><strong>' + esc(metricDelta(metric, kind)) + '</strong><small>A ' + metricValue(metric, kind, 'a') + '　B ' + metricValue(metric, kind, 'b') + '</small></article>'
}

function observedRow(label: string, metric: MetricDelta, kind: MetricKind) {
  const a = Math.abs(finite(metric.a) || 0); const b = Math.abs(finite(metric.b) || 0); const max = Math.max(1, a, b)
  return '<div class="observed-row"><span>' + esc(label) + '</span><div><i class="bar-a" style="width:' + (a / max * 100) + '%"></i><i class="bar-b" style="width:' + (b / max * 100) + '%"></i></div><b>' + esc(metricDelta(metric, kind)) + '</b></div>'
}

function renderObserved(report: ComparisonReport) {
  return '<div class="observed-grid"><article><h3>Token 与调用结构</h3><span>DERIVED FROM EXISTING METRIC BUNDLE</span>' + observedRow('Input Tokens', report.efficiency.inputTokens, 'tokens') + observedRow('Cached Input', report.efficiency.cachedInputTokens, 'tokens') + observedRow('Uncached Input', report.efficiency.uncachedInputTokens, 'tokens') + observedRow('Model Calls', report.efficiency.modelCalls, 'integer') + '</article><article><h3>上下文与稳定性</h3><span>CONTEXT / RELIABILITY</span>' + observedRow('Peak Context', report.context.peakRatio, 'ratio') + observedRow('Tool Failures', report.reliability.toolFailureRate, 'ratio') + observedRow('Compactions', report.context.compactions, 'integer') + observedRow('Runtime', report.efficiency.duration, 'duration') + '</article></div>'
}

function renderTrajectory(report: ComparisonReport) {
  const items = report.divergences.slice(0, 3)
  if (!items.length) return '<div class="empty">未发现显著轨迹结构差异 · DATA UNAVAILABLE</div>'
  return '<div class="trajectory-grid">' + items.map((item, index) => '<article><strong>' + String(index + 1).padStart(2, '0') + '</strong><span>' + esc(item.type.replaceAll('_', ' ').toUpperCase()) + '</span><p><b>A</b> ' + esc(item.a.sequence.join(' → ') || '—') + '<br><b>B</b> ' + esc(item.b.sequence.join(' → ') || '—') + '</p><small>' + esc(item.summary) + '</small></article>').join('') + '</div>'
}

const referenceCss = `
:root{--paper:#f7f3ed;--navy:#092967;--blue:#3456b8;--sky:#b7d9f0;--mid:#7099d0;--ink:#183675;--muted:#77839c;--line:#133777;--pale:#f0ede7;--warn:#c27055}*{box-sizing:border-box}html,body{background:var(--paper)}body{color:var(--navy);font-family:Arial,"PingFang SC","Microsoft YaHei",sans-serif;font-size:13px;line-height:1.48}.sheet{width:1080px;max-width:100%;margin:0 auto;padding:42px 50px 44px;background:var(--paper)}h1,h2,h3,p{margin:0}.topbar{height:28px;border-bottom:1px solid var(--line);display:flex;justify-content:space-between;font-size:12px;font-weight:800;color:var(--navy)}.topbar span:last-child{font-size:9px;color:var(--muted);letter-spacing:.03em}.report-hero{position:relative;display:grid;grid-template-columns:57% 43%;min-height:350px;padding:49px 0 37px;border-bottom:1px dashed var(--sky);overflow:hidden}.hero-copy{position:relative;z-index:1}.hero-copy h1{max-width:480px;font-size:52px;line-height:1.08;letter-spacing:-.055em;font-weight:800}.hero-copy .eyebrow{margin-top:6px;color:var(--blue);font-size:11px;font-weight:800}.hero-copy p{max-width:525px;margin-top:13px;color:var(--muted);font-size:16px;line-height:1.48}.hero-stats{display:flex;gap:28px;margin-top:30px}.hero-stats div{width:116px;border-top:1px solid var(--line);padding-top:10px}.hero-stats b{display:block;font-size:22px;line-height:1}.hero-stats span,.panel-title span,.observed-grid>article>span,.chart-note{display:block;color:var(--muted);font-size:8px;font-weight:700;letter-spacing:.03em}.hero-arc{position:absolute;right:0;bottom:8px;width:535px;height:294px}.hero-arc svg{width:100%;height:100%;overflow:visible}.hero-arc path{fill:none}.hero-arc .arc-light{stroke:var(--sky);stroke-width:2.5}.hero-arc .arc-mid{stroke:#779ee5;stroke-width:1.3}.hero-arc .arc-main{stroke:var(--blue);stroke-width:2}.hero-arc circle{fill:var(--blue)}.arc-label{position:absolute;background:var(--paper);border:1px solid var(--line);padding:6px 27px;color:var(--navy);font-size:9px;font-weight:800;white-space:nowrap}.arc-label.a{left:30px;bottom:43px}.arc-label.b{right:0;bottom:105px}.arc-label.verdict{left:223px;bottom:105px;color:var(--blue)}.validity-band{display:grid;grid-template-columns:1fr 178px;gap:30px;padding:31px 0 20px;border-bottom:1px solid var(--line)}.validity-copy h2{font-size:23px;line-height:1.1}.validity-copy .kicker{font-size:9px;letter-spacing:.04em;color:var(--muted);font-weight:800}.validity-copy p{max-width:690px;margin:13px 0 16px;color:var(--muted);font-size:13px}.validity-tags{display:flex;flex-wrap:wrap;gap:8px}.validity-tags span{border:1px solid var(--line);padding:6px 8px;font-size:9px;font-weight:800}.validity-tags .different{border-color:var(--warn);color:var(--warn)}.attribution{border-left:1px solid var(--sky);padding:10px 0 0 20px}.attribution span{display:block;color:var(--muted);font-size:8px;font-weight:800}.attribution b{display:block;margin-top:18px;font-size:32px;line-height:.9}.attribution em{font-size:11px;font-style:normal;color:var(--warn);font-weight:800}.overview{display:grid;grid-template-columns:minmax(0,1fr) 248px;gap:38px;padding:39px 0 31px;border-bottom:1px solid var(--line)}.overview h2,.observed-head h2,.trajectory-head h2{font-size:31px;line-height:1;letter-spacing:-.04em}.overview-title,.observed-head,.trajectory-head{display:flex;align-items:baseline;gap:24px;margin-bottom:19px}.overview-title span,.observed-head span,.trajectory-head span{font-size:8px;color:var(--muted);font-weight:800}.evidence-grid{display:grid;grid-template-columns:1fr 1fr;gap:18px}.evidence-panel{min-height:255px;background:rgba(9,41,103,.025);padding:14px}.panel-title{border-top:1px solid var(--line);padding-top:9px}.panel-title h3{font-size:15px;line-height:1.18}.measure-row{display:grid;grid-template-columns:92px 1fr 44px;gap:8px;align-items:center;margin-top:20px;font-size:10px;font-weight:700}.measure-track,.observed-row>div{height:10px;position:relative;background:var(--sky)}.measure-track:after{content:"";position:absolute;left:0;top:-4px;height:18px;border-left:1px solid var(--line)}.measure-track i{display:block;height:10px;background:var(--blue)}.measure-track i.up{background:var(--blue)}.measure-track i.down{background:var(--mid)}.measure-row b{text-align:right;font-size:10px}.stack-pair{margin:19px 0 9px}.stack-row{grid-template-columns:15px 1fr 0;gap:5px;margin:14px 0}.stack-row small{display:none}.stack{height:25px;background:var(--sky)}.legend{gap:8px 12px;font-size:8px}.legend i{width:8px;height:8px}.context-table,.annotation{display:none}.tool-sides{display:grid;grid-template-columns:1fr 1fr;gap:17px;margin-top:28px}.tool-sides>div{position:relative}.tool-sides b{font-size:12px}.tool-sides small{float:right;color:var(--muted);font-size:8px}.tool-sides p{display:flex;gap:4px;margin:15px 0 25px}.tool-dot{width:21px;height:21px;display:grid;place-items:center;border:1px solid var(--mid);border-radius:50%;color:var(--blue);font-size:11px;font-style:normal}.tool-dot.fail{border-color:var(--warn);color:var(--warn)}.tool-na{font-size:8px;color:var(--muted)}.tool-sides strong{display:block;font-size:29px;line-height:1}.tool-sides em{font-size:8px;color:var(--muted);font-style:normal}.mini-trace{display:block;width:100%;height:157px;margin-top:16px}.mini-trace line{stroke:var(--sky);stroke-width:1}.mini-trace polyline{fill:none;stroke-width:2.5}.mini-trace .trace-a{stroke:var(--blue)}.mini-trace .trace-b{stroke:var(--navy)}.signal-rail{border-top:1px solid var(--line)}.signal-rail h3{margin-top:-1px;padding-top:0;font-size:28px;line-height:.95}.signal-rail>p{margin:21px 0 25px;color:var(--muted);font-size:12px}.signal-grid{display:grid;grid-template-columns:1fr 1fr;gap:30px 17px}.signal-tile{min-height:111px;border-top:1px solid var(--line);border-bottom:1px solid var(--sky);padding-top:10px}.signal-tile b{display:block;font-size:11px}.signal-tile span{display:block;color:var(--muted);font-size:8px}.signal-tile strong{display:block;margin-top:26px;font-size:22px;line-height:1}.signal-tile small{display:block;margin-top:10px;color:var(--muted);font-size:7px}.observed{padding:31px 0 30px;border-bottom:1px solid var(--line)}.observed-grid{display:grid;grid-template-columns:1fr 1fr;gap:22px}.observed-grid article{min-height:205px;background:rgba(9,41,103,.025);padding:19px 16px}.observed-grid h3{font-size:15px}.observed-row{display:grid;grid-template-columns:110px 1fr 52px;gap:10px;align-items:center;margin-top:18px;font-size:10px;font-weight:700}.observed-row>div{overflow:hidden}.observed-row i{position:absolute;top:0;bottom:0;display:block}.bar-a{left:0;background:var(--blue);opacity:.95}.bar-b{right:0;background:var(--mid);opacity:.55}.observed-row b{text-align:right;font-size:10px}.observed-warning{margin-top:18px;border:1px solid var(--warn);padding:13px 16px;color:var(--warn);font-size:10px}.trajectory{padding:31px 0 21px;border-bottom:1px solid var(--line)}.trajectory-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:28px}.trajectory-grid article{min-height:146px;border-top:1px solid var(--line);padding-top:12px}.trajectory-grid strong{font-size:28px;color:var(--mid);letter-spacing:.03em}.trajectory-grid span{margin-left:14px;font-size:8px;font-weight:800;color:var(--muted)}.trajectory-grid p{margin-top:13px;font-size:11px;font-weight:700;line-height:1.8}.trajectory-grid small{display:block;margin-top:12px;padding-top:8px;border-top:1px dashed var(--sky);color:var(--muted);font-size:8px;line-height:1.55}.report-footer{display:flex;justify-content:space-between;padding-top:17px;font-size:10px;font-weight:800;color:var(--blue)}@media(max-width:760px){.sheet{padding:25px 20px}.report-hero{grid-template-columns:1fr;min-height:420px}.hero-copy h1{font-size:40px}.hero-arc{opacity:.38;width:440px;right:-90px}.validity-band,.overview{grid-template-columns:1fr}.attribution{border-left:0;border-top:1px solid var(--sky);padding:15px 0}.evidence-grid,.observed-grid,.trajectory-grid{grid-template-columns:1fr}.signal-rail{margin-top:15px}.hero-copy p{font-size:14px}.arc-label.b{right:40px}.report-footer{gap:12px;flex-wrap:wrap}}@media print{.sheet{width:1080px;padding:42px 50px}.report-hero,.validity-band,.overview,.observed,.trajectory{break-inside:avoid;page-break-inside:avoid}}`

const finalCss = '.signal-rail{border-top:0}.session-arc-diagram.hero-arc{position:static;right:auto;bottom:auto;display:block;align-self:end;width:100%;height:auto;min-width:0;overflow:visible;opacity:1}.session-arc-diagram .session-arc-summary{fill:var(--muted);font:700 10px Arial,"PingFang SC","Microsoft YaHei",sans-serif;letter-spacing:.04em}.session-arc-diagram .session-arc-line{fill:none;stroke-linecap:round;vector-effect:non-scaling-stroke}.session-arc-diagram .session-arc-line-light{stroke:var(--sky);stroke-width:1.4}.session-arc-diagram .session-arc-line-mid{stroke:var(--mid);stroke-width:2}.session-arc-diagram .session-arc-line-main{stroke:var(--blue);stroke-width:3}.session-arc-diagram .session-arc-baseline{fill:none;stroke:var(--line);stroke-width:1;stroke-dasharray:2 4;vector-effect:non-scaling-stroke}.session-arc-diagram .session-arc-anchor{vector-effect:non-scaling-stroke}.session-arc-diagram .session-arc-anchor-primary{fill:var(--navy);stroke:var(--navy);stroke-width:1}.session-arc-diagram .session-arc-anchor-focus{fill:var(--paper);stroke:var(--mid);stroke-width:1.5}.session-arc-diagram .session-arc-anchor-conclusion{fill:var(--paper);stroke:var(--blue);stroke-width:1.5}.session-arc-diagram .session-arc-label{fill:var(--navy);font:800 10px Arial,"PingFang SC","Microsoft YaHei",sans-serif;letter-spacing:.02em}.session-arc-diagram .session-arc-label-note{fill:var(--muted);font-size:8px;letter-spacing:.12em}.session-arc-diagram .session-arc-label-focus{fill:var(--mid)}.session-arc-diagram .session-arc-label-conclusion{fill:var(--blue)}@media(max-width:760px){.session-arc-diagram.hero-arc{width:100%;right:auto;opacity:1}}'

export function renderComparisonHtml(report: ComparisonReport): string {
  const safe = sanitizeComparisonReport(report)
  const invalid = safe.validity.level === 'invalid' || !safe.validity.attributionEnabled
  const tokenDelta = safe.efficiency.totalTokens.percentDelta
  const coreTitle = tokenDelta == null ? '速度与 Token 差异明显' : tokenDelta > 0 ? '速度更快，但 Token 明显更高' : tokenDelta < 0 ? '速度更快，Token 也明显更低' : '速度与 Token 基本持平'
  const compactionDelta = safe.context.compactions.absoluteDelta
  const contextTitle = compactionDelta == null || compactionDelta === 0 ? '上下文压力与调用节奏不同' : compactionDelta < 0 ? 'A 发生压缩，B 保持平稳增长' : 'B 发生压缩，A 保持平稳增长'
  const validityItems = safe.validity.dimensions.map(item => '<span class="' + item.relation + '">' + esc(item.label) + ' · ' + esc(item.relation === 'same' ? '相同' : item.relation === 'different' ? '不同' : '未知') + '</span>').join('')
  const reportJson = JSON.stringify(safe).replace(/</g, '\\u003c').replace(/>/g, '\\u003e').replace(/&/g, '\\u0026').replace(/\u2028/g, '\\u2028').replace(/\u2029/g, '\\u2029')
  const html = [
    '<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Agent Workbench · Session Comparison Report</title><style>', css, referenceCss, finalCss, '</style></head><body><main class="sheet">',
    '<header class="topbar"><span>AGENT WORKBENCH / SESSION COMPARE</span><span>PORCELAIN REPORT · VISUAL DRAFT</span></header>',
    '<section class="report-hero"><div class="hero-copy"><h1>两条 Session，<br>哪里真的不同？</h1><div class="eyebrow">AGENT SESSION COMPARISON REPORT</div><p>把 Trace、Token、上下文、工具调用与结果证据压缩成一页可判断的分析。报告先给结论，再给证据，最后给优化动作。</p><div class="hero-stats"><div><b>' + metricValue(safe.efficiency.duration, 'duration', 'a') + '</b><span>BASELINE A</span></div><div><b>' + metricValue(safe.efficiency.duration, 'duration', 'b') + '</b><span>CANDIDATE B</span></div></div></div>' + heroArc(safe, invalid) + '</section>',
    '<section class="validity-band"><div class="validity-copy"><h2>这次对比' + (invalid ? '不能用于 Harness 归因。' : '具备归因条件。') + '</h2><div class="kicker">COMPARISON VALIDITY</div><p>' + esc(safe.validity.summary) + '</p><div class="validity-tags">' + validityItems + '</div></div><aside class="attribution"><span>ATTRIBUTION</span><b>' + (invalid ? 'INVALID' : esc(safe.validity.level.toUpperCase())) + '</b><em>' + (invalid ? '仅供观察性比较' : formatPercent(safe.validity.confidence)) + '</em></aside></section>',
    '<section class="overview"><div><div class="overview-title"><h2>一眼判断</h2><span>AT A GLANCE</span></div><div class="evidence-grid"><article class="evidence-panel"><div class="panel-title"><h3>' + esc(coreTitle) + '</h3><span>COMPARATIVE DELTA · OBSERVED METRICS</span></div>' + compactDeltaRows([{ label: '执行时间', metric: safe.efficiency.duration, kind: 'duration' }, { label: '总 Token', metric: safe.efficiency.totalTokens, kind: 'tokens' }, { label: 'TTFT', metric: safe.efficiency.ttft, kind: 'duration' }, { label: '峰值上下文', metric: safe.context.peakRatio, kind: 'ratio' }]) + '</article><article class="evidence-panel"><div class="panel-title"><h3>Prompt 结构完全不同</h3><span>CONTEXT COMPOSITION · KNOWN FOOTPRINT</span></div>' + renderContext(safe) + '</article><article class="evidence-panel">' + renderReliabilityPanel(safe) + '</article><article class="evidence-panel">' + renderContextTracePanel(safe, contextTitle) + '</article></div></div><aside class="signal-rail"><h3>关键起点</h3><span class="chart-note">A STARTING POINT</span><p>报告先用可比较的派生指标建立判断，再下钻到 Trace、原始 Bundle 与结构差异报告。</p><div class="signal-grid">' + signalTile('执行时间', 'Runtime', safe.efficiency.duration, 'duration') + signalTile('总 Token', 'Total tokens', safe.efficiency.totalTokens, 'tokens') + signalTile('缓存命中', 'Cache hit', safe.efficiency.cacheHitRatio, 'ratio') + signalTile('失败率', 'Tool failure', safe.reliability.toolFailureRate, 'ratio') + signalTile('峰值上下文', 'Peak context', safe.context.peakRatio, 'ratio') + signalTile('Prompt 足迹', 'Known footprint', { ...safe.context.peakRatio, a: safe.context.baseline.promptFootprintTotal, b: safe.context.candidate.promptFootprintTotal, absoluteDelta: (safe.context.candidate.promptFootprintTotal || 0) - (safe.context.baseline.promptFootprintTotal || 0), percentDelta: safe.context.baseline.promptFootprintTotal ? ((safe.context.candidate.promptFootprintTotal || 0) - safe.context.baseline.promptFootprintTotal) / safe.context.baseline.promptFootprintTotal : null, percentagePointDelta: null }, 'tokens') + '</div></aside></section>',
    '<section class="observed"><div class="observed-head"><h2>观察到的差异</h2><span>MEASURED DELTA · NOT CAUSAL ATTRIBUTION</span></div>' + renderObserved(safe) + (invalid ? '<p class="observed-warning">当前样本的 Task、Harness 或 Environment 不一致，因此这里只展示可测量的 Delta，不生成 Cost Driver Attribution；待 Comparison Validity 通过后再输出因果归因。</p>' : '') + '</section>',
    '<section class="trajectory"><div class="trajectory-head"><h2>轨迹结构差异</h2><span>TRAJECTORY DIFF · OBSERVED CHANGES</span></div>' + renderTrajectory(safe) + '</section>',
    '<footer class="report-footer"><span>下一步：先做可归因对照，再做 Driver 与 Recommendations。</span><span>AGENT WORKBENCH · REPORT V1</span></footer>',
    '<script id="agent-workbench-report-data" type="application/json">', reportJson, '</script></main></body></html>',
  ].join('')
  return html
}
